# SDE Resume Builder — Python dependencies
#
# Renderer (engine/main.py + renderer/render_pdf.py):
weasyprint==70.0
python-docx==1.2.0
jinja2==3.1.6
# Reference scanner (tools/scan.py):
pymupdf==1.28.2

# Optional: install the cm-super / texlive-fonts-recommended system package so
# the PDF renders with the reference font family instead of a Times fallback.
