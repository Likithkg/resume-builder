# Reference resumes

These PDFs exist for **visual reference only** — they define the layout, typography, density, and
section geometry the builder must reproduce. They contain **NO candidate facts**. Never transfer a
name, employer, date, or project from here into a resume; the candidate profile (`docs/`) is the
only source of truth for content.

